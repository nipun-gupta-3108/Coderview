# dashboard/

Intentionally empty as of Sprint 0.

Per `docs/DEVELOPMENT_ROADMAP.md`, **Sprint 3 — Dashboard Feature Migration** adds:

- `components/SessionCard.jsx` (unifies `ActiveSessions`/`RecentSessions` per a `variant` prop)
- `components/SessionList.jsx`
- `components/WelcomeSection.jsx`
- `components/StatsCards.jsx`
- `CreateSessionFlow/CreateSessionModal.jsx`
- `hooks/useDashboardData.js`

This file exists only to keep the folder tracked in git until it has real contents.
