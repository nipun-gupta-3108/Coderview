# primitives/

Intentionally empty as of Sprint 0.

Per `docs/DEVELOPMENT_ROADMAP.md`:

- **Sprint 1** adds the static primitives: `Panel`, `IconChip`, `MiniLabel`, `StatusChip`, `SectionKicker`, `Badge`.
- **Sprint 2** adds the interactive/layout primitives: `Button`, `Select`, `Modal`.

These wrap the existing CSS classes in `frontend/src/index.css` (`surface-panel`, `icon-chip`, `status-chip`, etc.) — see `docs/DESIGN_SYSTEM.md` §6–§9 for the spec each primitive must match exactly.

This file exists only to keep the folder tracked in git until it has real contents.
