# icons/

Intentionally empty as of Sprint 0.

Reserved per `docs/PROJECT_ARCHITECTURE.md` §5 for "a thin wrapper around `lucide-react` if we need consistent sizing." Icon sizing tokens already exist as a documented convention in `docs/DESIGN_SYSTEM.md` §18 (16/20/24/28–32px) without a wrapper component today.

This folder is **not** scheduled in a specific sprint in `docs/DEVELOPMENT_ROADMAP.md`. It should only be populated if icon-sizing drift becomes a real problem during later sprints — don't add a wrapper speculatively.

This file exists only to keep the folder tracked in git until (or unless) it has real contents.
