# layout/

Intentionally empty as of Sprint 0.

Per `docs/DEVELOPMENT_ROADMAP.md`, **Sprint 2** adds:

- `ResizableSplit` — thin wrapper around `react-resizable-panels`, baking in the gradient `PanelResizeHandle` styling currently duplicated in `ProblemPage.jsx` / `SessionPage.jsx`. See `docs/DESIGN_SYSTEM.md` §13.
- `PageShell` — wraps the existing `page-wrap` class.

This file exists only to keep the folder tracked in git until it has real contents.
